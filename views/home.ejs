<%- include('partials/header'); -%>


<%# comment: truncating each post to the first 50 characters thats what .substr is for, so home page is less cluttered. so i tagged on the .substr method at the end of the content. Also to dynamicly render the page i added the link in href in ejs tags %>

<h1>Home</h1>
<p> <%= startingContent %></p>
	
			<% posts.forEach(function(element) { %>
		<h1> <%=	 element.title %> </h1>

		<p>
		<%=element.content.substr(0,100) + "..."%>
		<a href="posts/<%=element._id%>">Read More</a>
		</p>

			<% }); %>


<%- include('partials/footer'); -%>
